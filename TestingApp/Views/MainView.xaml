<UserControl x:Class="FileSorter.Views.MainView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <!-- Content area -->
            <RowDefinition Height="*" />
            <!-- Button at bottom -->
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" HorizontalAlignment="Stretch" VerticalAlignment="Top">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <Button Grid.Column="0" Content="Settings" Command="{Binding OpenSettingsCommand}" Margin="10" Height="40" Width="300" FontWeight="Bold"/>
            <Button Grid.Column="1" Content="Profiles" Command="{Binding OpenProfilesCommand}" Margin="10" Height="40" Width="300" FontWeight="Bold"/>
        </Grid>

        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Visible">
            <TextBlock x:Name="OutputTB" Text="{Binding StatusText}" Margin="10" TextWrapping="Wrap" />
        </ScrollViewer>

        <Button Grid.Row="2" Content="Sort" Margin="20" Command="{Binding SortCommand}" Height="40" Grid.ColumnSpan="2" FontWeight="Bold"/>

    </Grid>
</UserControl>
